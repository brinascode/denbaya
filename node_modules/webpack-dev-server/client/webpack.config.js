'use strict';

const UglifyJSPlugin = require('uglifyjs-webpack-plugin');

module.exports = {
  module: {
    rules: [
      {
        test: /\.pug$/,
        use: [
          'pug-loader?self'
        ]
      },
      {
        test: /\.css$/,
        use: [
          'style-loader',
          'css-loader'
        ]
      },
      devServer: {
        disableHostCheck: true,
        host: '0.0.0.0',
        port: 3000
       }
    ]
  },
  plugins: [
    new UglifyJSPlugin()
  ]
};
